<?xml version="1.0" encoding="utf-8"?>

<ivysettings>

    <typedef name="svn" classname="fm.last.ivy.plugins.svnresolver.SvnResolver"/>

    <resolvers>
        <svn name="yandex-ivy-svn" 
             userName="ivy-robot" 
             userPassword="LAme12ljm" 
             repositoryRoot="https://svn.yandex.ru/ivy"
             binaryDiff="false">
             <ivy pattern="repo/trunk/[organisation]/[module]/ivys/ivy-[revision].xml"/>
             <artifact pattern="repo/trunk/[organisation]/[module]/[revision]/[type]s/[artifact].[ext]"/>
        </svn>
        
        <ibiblio name="ibiblio" m2compatible="true"/>
    </resolvers>

    <settings defaultResolver="ibiblio"/>
    
</ivysettings>

<!-- vim: set ts=4 sw=4 et: -->
